# MatBook Assignment — Final (React JSX + TanStack + Tailwind + Node.js)

This project implements the MatBook assignment per the provided PDF:

- Frontend: React 19 , TanStack Query, TanStack Form TanStack Table, Tailwind CSS , ShadCN-style components
- Backend: Node.js + Express,
- All 8 field types supported and validations implemented (min/max, regex, minDate, minSelected, required)
- Submissions listing supports server-side pagination and sorting.

## Run

### Backend

cd backend
npm install
npm run dev

### Frontend

cd frontend
npm install
npm run dev

The frontend Vite server proxies /api to http://localhost:4000 by default.
