{
  "name": "matbook-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "(cd backend && npm ci) && (cd frontend && npm ci --legacy-peer-deps && npm run build)",
    "start": "cd backend && npm start"
  }
}
